import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  iconContainer: {
    alignItems: 'center',
    height: '100%',
    justifyContent: 'center',
    position: 'absolute',
    width: 55,
  },
  mainContainer: {
    alignItems: 'center',
    height: 50,
    justifyContent: 'center',
    width: '100%',
  },
  title: {
    fontSize: 16,
  },
});
