import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    flex: 1,
  },
  topMenuTitle: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
    marginBottom: 5,
    flexDirection: 'row',
    borderColor: 'white',
    borderWidth: 0,
  },
});
